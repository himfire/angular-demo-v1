export interface Lesson{
  id ?:number;
  topic ?:string,
  name ?:string,
  img ?:string,
  price ?:number,
  title ?:string,
  instructor ?:string
  numberOfStudents ?:number,
  numberOfComments ?:number
}
